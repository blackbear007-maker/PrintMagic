torch==2.3.1
torchvision==0.18.1
Pillow==10.3.0
numpy==1.26.4

# Retinexformer (enhance/low-light) — only needs its own model file (retinexformer_arch.py,
# vendored into this image) plus einops for its attention reshape ops; does not need basicsr or
# the rest of the Retinexformer repo's training/eval harness.
einops==0.8.2

# Real-ESRGAN (upscale) — realesrgan's own PyPI release still hard-depends on basicsr, which is
# unmaintained (last PyPI release 2022-08-30) and imports a torchvision module removed in 0.17+.
# The Dockerfile patches that one broken import after install — see the RUN sed step there.
realesrgan==0.3.0
basicsr==1.4.2
opencv-python-headless==4.10.0.84

# ARNIQA (quality assessment) — loaded via torch.hub, no extra pip package needed beyond torch/
# torchvision above.

# LaMa (inpaint) — the checkpoint is a TorchScript-traced export (torch.jit.load), so it needs
# no architecture class or extra package at all beyond torch/numpy/Pillow already above.

# rembg (matting) — pulls in its own dependency chain (pymatting, scikit-image, numba, pooch,
# jsonschema) noticeably heavier than any other model added to this image so far. Does NOT
# declare onnxruntime as a hard dependency in its own metadata — verified 2026-08-27 by installing
# rembg alone and hitting `ModuleNotFoundError: No module named 'onnxruntime'` at import time —
# so it's pinned explicitly here rather than relying on it being pulled in transitively.
rembg==2.0.69
onnxruntime==1.29.0

# YuNet (detect-face) — loaded via cv2.FaceDetectorYN, part of opencv-python-headless's DNN
# module (already listed above for the Real-ESRGAN/basicsr patch) — no new package needed.
