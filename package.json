{
  "name": "printmagic",
  "version": "3.0.0",
  "private": true,
  "description": "Professional client-side print-ready image converter and preparation studio for AI artwork. 100% offline, zero data upload.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "jspdf": "^2.5.2"
  },
  "devDependencies": {
    "@types/jspdf": "^2.0.0",
    "@types/node": "^22.13.4",
    "typescript": "^5.7.3",
    "vite": "^6.1.0",
    "vitest": "^3.0.5"
  }
}